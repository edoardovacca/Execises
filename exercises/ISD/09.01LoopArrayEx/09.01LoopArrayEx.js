/*Write a loop that writes all the array element values into the browser console. 
Each value should be displayed on a separate line*/

var array = ["Pekka", "Minna", "Heikki", "Anna", "Jukka", "Iida"];

function loopArray() {
    // TODO
    for(var i = 0; i < array.length; i++){
        console.log(array[i]);
        }
}