// Insert your JavaScript code here
/*A function can also return a value. For example, you can assign the return value to a variable.

The function Math.random() returns a value from the range [0, 1[. 
Call the function and assign the return value to the variable randomizedNumber. 
After that, write the value of the variable into the browser console.
*/

var randomizedNumber = Math.random();
console.log(randomizedNumber);
